apiVersion: v1
kind: Service
metadata:
  labels:
    app: kargo-demo
  name: kargo-demo-canary
spec:
  ports:
  - name: http
    port: 3000
    protocol: TCP
    targetPort: 8080
  selector:
    app: kargo-demo
  type: ClusterIP
