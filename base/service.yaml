apiVersion: v1
kind: Service
metadata:
  name: kargo-demo
  labels:
    app: kargo-demo
spec:
  type: NodePort
  selector:
    app: kargo-demo
  ports:
  - name: http
    protocol: TCP
    port: 3000
    targetPort: 8080
