apiVersion: v1
kind: Service
metadata:
  labels:
    app: kargo-demo
  name: kargo-demo
spec:
  ports:
  - name: http
    nodePort: 30088
    port: 3000
    protocol: TCP
    targetPort: 8080
  selector:
    app: kargo-demo
  type: NodePort
