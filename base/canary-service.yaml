apiVersion: v1
kind: Service
metadata:
  name: kargo-demo-canary
  labels:
    app: kargo-demo
spec:
  type: ClusterIP
  selector:
    app: kargo-demo
  ports:
  - name: http
    protocol: TCP
    port: 3000
    targetPort: 8080
